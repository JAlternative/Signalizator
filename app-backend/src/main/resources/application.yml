spring:
  application:
    name: app-backend
  datasource:
    url: jdbc:postgresql://localhost:5433/agent_platform
    username: agent_platform_user
    password: agent_platform_password
    driver-class-name: org.postgresql.Driver

management:
  endpoints:
    web:
      exposure:
        include: health,info